{
  "_comment": [
    "Independent fitness table for build_dispatch_plan()'s _select_workflow(),",
    "authored per Proposal 03 of the orchestration review.",
    "",
    "PURPOSE: selection_golden_corpus.json pins build_dispatch_plan()'s current",
    "output as its own regression baseline -- it is derived FROM the code and",
    "therefore cannot catch the code mislabeling something, because a bug in",
    "_select_workflow() and a fixture copied from that same buggy output will",
    "always agree. This file is deliberately NOT derived from _select_workflow()",
    "or from the golden corpus. Every 'expected_workflow' value below was",
    "reasoned independently from roster/workflows/*.md (the human-authored",
    "description of what each workflow shape means) and from routing.yaml's",
    "route/risk semantics, then checked against selection.schema.json's closed",
    "'workflow' enum. Where an entry's task/changed_files were reused from",
    "selection_golden_corpus.json or test_selector.py (to guarantee a known",
    "route/risk match without re-deriving routing.yaml's matching rules), the",
    "'reasoning' field says so explicitly -- reuse of INPUT text is fine;",
    "copying the EXPECTED OUTPUT would defeat the point of this file.",
    "",
    "test_workflow_fitness_table.py runs build_dispatch_plan() against every",
    "case here and asserts actual workflow == expected_workflow. Two cases are",
    "deliberately expected to disagree with current code (see their 'reasoning'",
    "and 'known_mismatch' fields) -- that disagreement is the entire point of",
    "this file: it is proof the table is independent, not a copy of the bug.",
    "",
    "Coverage: at least one case per selection.schema.json workflow enum value",
    "(new-service, product-intake, runtime-assurance, debugging,",
    "infrastructure-change, pipeline-change, knowledge-ingestion,",
    "support-escalation, production-release, rollback, unclassified,",
    "needs-triage).",
    "",
    "Scope boundary: this file does not add a new workflow enum value, does",
    "not modify _select_workflow(), and does not add a roster/workflows/*.md",
    "doc. If a case below implies the closed enum is genuinely missing a",
    "shape, that is Proposal 08's territory, not this file's -- see the PR",
    "description for which cases (if any) that applies to."
  ],
  "cases": [
    {
      "id": "WF-NEW-SERVICE-1",
      "description": "backend-only build route -> new-service",
      "task": "Implement a backend API for user upload",
      "changed_files": ["services/upload/main.go"],
      "classification": "internal",
      "task_id": "FITNESS-NEW-SERVICE-1",
      "expected_workflow": "new-service",
      "reasoning": "new-service.md: 'covers work that proceeds beyond intake into design and delivery.' A backend route match (one of the build-shaped routes: frontend/backend/infrastructure/pipeline) implementing a new API endpoint is squarely design-and-delivery work, not a narrower recognized shape (not infra/pipeline-only, not a defect fix, not intake-only). Task/files reused verbatim from selection_golden_corpus.json's BACKEND-1 to guarantee the route match; the expected workflow value was derived independently from new-service.md, not copied from BACKEND-1's (unasserted) workflow."
    },
    {
      "id": "WF-NEW-SERVICE-ARCHITECTURE-RISK-1",
      "description": "architecture-change risk with no build-shaped route -> new-service",
      "task": "Design the architecture and system design proposal",
      "changed_files": ["architecture/design.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-NEW-SERVICE-2",
      "expected_workflow": "new-service",
      "reasoning": "new-service.md step 3 names the architecture phase (Cloud Architect, G3) as an explicit stage of this same lifecycle shape, so a task that trips routing.yaml's 'architecture-change' risk rule (System Architect boundary/ADR work) belongs to new-service even when no frontend/backend/infrastructure/pipeline route also matched. Task/files reused verbatim from selection_golden_corpus.json's ARCHITECTURE-DESIGN-1."
    },
    {
      "id": "WF-PRODUCT-INTAKE-1",
      "description": "product-intent route only -> product-intake",
      "task": "Capture the product intent for the new mission objective",
      "changed_files": ["product/intent/scope.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-PRODUCT-INTAKE-1",
      "expected_workflow": "product-intake",
      "reasoning": "product-intake.md: 'Use this workflow when work has not yet entered architecture or implementation.' Drafting the versioned intent record is literally step 2 of product-intake.md, before any G2/architecture handoff. Task/files reused verbatim from selection_golden_corpus.json's PRODUCT-INTENT-1."
    },
    {
      "id": "WF-PRODUCT-INTAKE-2",
      "description": "requirements-baseline route only -> product-intake",
      "task": "Perform requirements decomposition and requirements traceability",
      "changed_files": ["product/requirements/baseline.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-PRODUCT-INTAKE-2",
      "expected_workflow": "product-intake",
      "reasoning": "product-intake.md step 4: Requirements Agent decomposes approved intent into requirement IDs/acceptance criteria/traceability -- the requirements-baseline route's own G1/G2 quality_gates in routing.yaml match product-intake.md's G1/G2 handoff, still strictly before new-service.md's G3 architecture stage. Task/files reused verbatim from selection_golden_corpus.json's REQUIREMENTS-BASELINE-1."
    },
    {
      "id": "WF-RUNTIME-ASSURANCE-1",
      "description": "runtime-assurance route only -> runtime-assurance",
      "task": "Observe production runtime conformance for deployed behavior",
      "changed_files": ["runtime-assurance/notes.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-RUNTIME-ASSURANCE-1",
      "expected_workflow": "runtime-assurance",
      "reasoning": "runtime-assurance.md's own flowchart (Observability SRE observes -> Security/Compliance review -> G10 Service Owner decides) is precisely this task's shape. Task/files reused verbatim from selection_golden_corpus.json's RUNTIME-ASSURANCE-1."
    },
    {
      "id": "WF-DEBUGGING-1",
      "description": "backend+testing+debugging routes, explicit defect investigation -> debugging",
      "task": "Debug a panic and identify the root cause from the stack trace",
      "changed_files": ["services/internal/repository/regression/panic_test.go"],
      "classification": "internal",
      "task_id": "FITNESS-DEBUGGING-1",
      "expected_workflow": "debugging",
      "reasoning": "debugging.md step 2: 'Reproduce the issue or prove it is unreproducible, isolate the smallest likely cause.' This task is a textbook reproduce/root-cause/fix case -- an actual defect, not routine maintenance (contrast with WF-AGENT-SUITE-GOVERNANCE-MISLABEL-1 below). Task/file reused verbatim from selection_golden_corpus.json's DEBUGGING-1."
    },
    {
      "id": "WF-INFRASTRUCTURE-CHANGE-1",
      "description": "infrastructure route only -> infrastructure-change",
      "task": "Update Terraform for a Proxmox worker VM",
      "changed_files": ["terraform/modules/worker/main.tf"],
      "classification": "internal",
      "task_id": "FITNESS-INFRASTRUCTURE-CHANGE-1",
      "expected_workflow": "infrastructure-change",
      "reasoning": "infrastructure-change.md's Classify -> Provision+Plan -> Infrastructure Reviewer -> gated approval -> Apply shape is exactly an IaC-only change with no frontend/backend/pipeline overlap. Task/files reused verbatim from selection_golden_corpus.json's INFRA-1.",
      "known_gate": "infrastructure-change.md cross-checks G3/G4/G5/G6/G7/G8 against routing.yaml's infrastructure route -- consistent with no risk rule firing here."
    },
    {
      "id": "WF-PIPELINE-CHANGE-1",
      "description": "pipeline route only -> pipeline-change",
      "task": "Change the GitLab pipeline runner configuration",
      "changed_files": [".gitlab-ci.yml"],
      "classification": "internal",
      "task_id": "FITNESS-PIPELINE-CHANGE-1",
      "expected_workflow": "pipeline-change",
      "reasoning": "pipeline-change.md's Document -> Test -> Pipeline Security Reviewer -> gated approval -> Enable shape covers exactly a CI/CD runner-configuration change. Task/files reused verbatim from selection_golden_corpus.json's PIPELINE-1."
    },
    {
      "id": "WF-KNOWLEDGE-INGESTION-1",
      "description": "knowledge-store route only -> knowledge-ingestion",
      "task": "Tune the knowledge store retrieval and embedding vector search",
      "changed_files": ["roster/knowledge-store/notes.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-KNOWLEDGE-INGESTION-1",
      "expected_workflow": "knowledge-ingestion",
      "reasoning": "knowledge-ingestion.md's Record -> Security/Compliance review -> Normalize -> Redact -> Ingest -> Evaluate -> Evidence shape is the recognized shape for knowledge-store-only work; nothing else (no frontend/backend/infra/pipeline/debugging) also matched. Task/files reused verbatim from selection_golden_corpus.json's KNOWLEDGE-STORE-1."
    },
    {
      "id": "WF-SUPPORT-ESCALATION-1",
      "description": "support route only -> support-escalation",
      "task": "Provide user report triage and a workaround for a customer issue",
      "changed_files": ["support/ticket-123.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-SUPPORT-ESCALATION-1",
      "expected_workflow": "support-escalation",
      "reasoning": "support-escalation.md's Triage -> Investigate -> Escalate -> Closure shape is exactly a customer-ticket triage/workaround task. Task/files reused verbatim from selection_golden_corpus.json's SUPPORT-1."
    },
    {
      "id": "WF-SUPPORT-ESCALATION-INCIDENT-1",
      "description": "incident-response route, task mentions 'rollback' but the action described is coordination/escalation, not execution -> support-escalation, NOT rollback",
      "task": "Coordinate rollback during a sev1 major incident postmortem",
      "changed_files": ["postmortems/2026-07-28.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-SUPPORT-ESCALATION-2",
      "expected_workflow": "support-escalation",
      "reasoning": "Deliberately placed next to WF-ROLLBACK-GAP-1 below to show the two are genuinely different shapes, not the same shape mislabeled two different ways. This task's verb is 'coordinate' during a sev1/postmortem/war-room incident -- that is support-escalation.md's Triage/Investigate/Escalate/'Human escalation' shape (an accountable human decides on the production action while the incident is being managed). rollback.md instead describes the release engineer's concrete execution playbook ('Release engineer selects the pre-approved rollback... obtain emergency authorization... deploy known-good artifact') once a decision has already been made. Incident coordination is not artifact-rollback execution, so support-escalation is the correct label for this specific task text even though it contains the word 'rollback'. Task/files reused verbatim from selection_golden_corpus.json's INCIDENT-RESPONSE-1."
    },
    {
      "id": "WF-PRODUCTION-RELEASE-1",
      "description": "infrastructure route + production risk -> production-release",
      "task": "Deploy to production with Terraform",
      "changed_files": ["terraform/service/main.tf"],
      "classification": "internal",
      "task_id": "FITNESS-PRODUCTION-RELEASE-1",
      "expected_workflow": "production-release",
      "reasoning": "production-release.md's G8 precondition-check -> G9 deploy-immutable-artifact -> verify -> G10 shape matches routing.yaml's 'production' risk rule (deploy/release/production keyword groups) exactly. Task/files reused verbatim from selection_golden_corpus.json's PRODUCTION-RISK-1."
    },
    {
      "id": "WF-ROLLBACK-GAP-1",
      "description": "KNOWN MISMATCH: an explicit rollback-execution task; 'rollback' is a real enum value in selection.schema.json but _select_workflow() has no branch that can ever return it",
      "task": "Roll back the production release to the last known-good artifact after failed verification",
      "changed_files": ["terraform/service/main.tf"],
      "classification": "internal",
      "task_id": "FITNESS-ROLLBACK-GAP-1",
      "expected_workflow": "rollback",
      "known_mismatch": true,
      "reasoning": "rollback.md's Trigger -> Preserve Evidence -> Release Engineer selects known-good procedure -> emergency human authorization -> Deploy known-good artifact -> Verify shape is exactly this task: a rollback triggered by a failed verification, executed against a known-good artifact. But build_dispatch_plan.py's _select_workflow() (roster/orchestration/src/build_dispatch_plan.py) has no branch that returns 'rollback' at all -- grep the function and it is not reachable from any route/risk combination. Concretely: this task's changed_files/task text match the 'infrastructure' route, and because the wording also trips routing.yaml's 'production' risk rule (group1 'production' + group2 'release'), the 'if \"production\" in risk_ids: return \"production-release\"' branch fires FIRST (build_dispatch_plan.py lines ~121-122) and short-circuits before any rollback-specific check could run even if one existed. This is a distinct bug from the agent-suite-governance mislabeling below: 'rollback' is dead code in the schema's closed enum, not a case of the wrong existing value being picked. It does not require a new enum value (rollback already exists in selection.schema.json) so it is in scope for this proposal, not Proposal 08 -- see the PR description."
    },
    {
      "id": "WF-UNCLASSIFIED-1",
      "description": "governance-planning route only, no recognized shape -> unclassified",
      "task": "Assess governance impact and data jurisdiction",
      "changed_files": ["governance/impact/notes.yaml"],
      "classification": "internal",
      "task_id": "FITNESS-UNCLASSIFIED-1",
      "expected_workflow": "unclassified",
      "reasoning": "unclassified.md: emitted when 'the selector matched at least one route or risk rule... but the matched combination does not fit any of this repository's recognized workflow shapes.' governance-planning is a narrow advisory/review route with its own AGENT.md but no dedicated roster/workflows/*.md shape (it contributes to new-service.md step 2 as one participant among several, but is never itself a whole recognized shape). Task/files reused verbatim from selection_golden_corpus.json's GOVERNANCE-PLANNING-1."
    },
    {
      "id": "WF-NEEDS-TRIAGE-1",
      "description": "no route/risk match -> needs-triage",
      "task": "Investigate an unexplained issue",
      "changed_files": ["unknown/file.xyz"],
      "classification": "internal",
      "task_id": "FITNESS-NEEDS-TRIAGE-1",
      "expected_workflow": "needs-triage",
      "reasoning": "AGENTS.md/RUNBOOK.md: 'If no rule matches a task, the selector returns needs-triage rather than guessing.' Trivial by construction: no selected agents means _select_workflow()'s first check (`if not has_agents: return \"needs-triage\"`) is the only defensible answer. Task/files reused verbatim from selection_golden_corpus.json's NEEDS-TRIAGE-1."
    },
    {
      "id": "WF-AGENT-SUITE-GOVERNANCE-MISLABEL-1",
      "description": "KNOWN MISMATCH: the review's named case -- routine roster/catalog maintenance, no defect present, currently labeled 'debugging'",
      "task": "add a new specialist role to the roster",
      "changed_files": ["roster/catalog.yaml", "roster/planning/new-role/AGENT.md"],
      "classification": "internal",
      "task_id": "FITNESS-AGENT-SUITE-GOV-1",
      "expected_workflow": "unclassified",
      "known_mismatch": true,
      "reasoning": "routing.yaml's agent-suite-governance route spans several intrinsically different kinds of work under one route id: adding a role (build/maintenance), packaging the provider (build/maintenance, see selection_golden_corpus.json's AGENT-SUITE-GOVERNANCE-1), fixing a routing bug (an actual defect), or auditing repo structure (governance review). Adding a new role has no defect to reproduce, so debugging.md's reproduce/root-cause/fix shape does not describe it. None of the other recognized shapes fit routine roster maintenance either (it is not a service being designed/delivered end-to-end per new-service.md's G1-G10 chain, not pre-architecture intent capture, not an infra/pipeline/production/rollback/support/knowledge-ingestion/runtime-assurance task). Per unclassified.md's own definition this is the correct current-enum label. But build_dispatch_plan.py's _select_workflow() (roster/orchestration/src/build_dispatch_plan.py, lines ~131-132) returns 'debugging' unconditionally for every agent-suite-governance route match, with no check for whether the task is actually a defect fix -- so this fixture, and the existing golden-corpus fixture AGENT-SUITE-GOVERNANCE-1, are both mislabeled today. A dedicated 'agent-suite-maintenance' workflow value would describe roster/catalog upkeep more precisely than 'unclassified', but adding that value is Proposal 08's scope, not this proposal's -- this row intentionally stays inside the existing closed enum and lets 'unclassified' stand as the best available answer until that follow-up lands."
    },
    {
      "id": "WF-NEW-SERVICE-CROSS-STACK-1",
      "description": "two build-shaped routes (frontend+backend) -> new-service",
      "task": "Add a React upload form backed by a PostgreSQL API",
      "changed_files": ["frontend/src/Upload.tsx", "services/upload/main.go"],
      "classification": "internal",
      "task_id": "FITNESS-NEW-SERVICE-3",
      "expected_workflow": "new-service",
      "reasoning": "Same reasoning as WF-NEW-SERVICE-1, extended to confirm a multi-route build case (frontend+backend) still resolves to the single new-service shape rather than being treated as ambiguous. Task/files reused verbatim from selection_golden_corpus.json's CROSS-STACK-1."
    }
  ]
}
